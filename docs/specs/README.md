# Specs

Design documents for features and architecture decisions. Written before implementation for
anything non-trivial, following `spec-template.md`.

## Index

- [KWG-INSTALL-P9ZT4-guild-module-install](./KWG-INSTALL-P9ZT4-guild-module-install.md) — Guild
  Module & Install Library.
- [KWG-ECO-K2N7Q-krewire-native-guild-template](./KWG-ECO-K2N7Q-krewire-native-guild-template.md) —
  Krewire-Native Guild Template.

The authoritative index with statuses is [`index.md`](./index.md); new specs are
added to both lists when written.

## Naming

File name: `{ProjectId}-{Scope}-{SpecID}-{slug}.md`. One spec per initiative.
`{ProjectId}` is the repo prefix (`KWG` here), `{Scope}` a short category,
`{SpecID}` a unique random 5-character alphanumeric code, `{slug}` a
kebab-case title. See the root `AGENTS.md` for the full convention.
