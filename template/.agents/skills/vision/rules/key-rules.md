# Vision — Key Rules

- Load `docs/project-vision.md` first — it is the compressed workload matrix (9 rows) and roadmap.
- Do not re-read all specs per task; only load the specific `KWF-*` spec for the slice you touch.
- Summarize vision in 5 lines when delegating to subagents to save context.
- Route to `runtime`/`infra`/`service`/`worker` subagents based on workload; `build` orchestrates cross-cutting work.
- Centralized specs live at `docs/specs/` (workspace `internal/` == repo `krewire/internal`).
