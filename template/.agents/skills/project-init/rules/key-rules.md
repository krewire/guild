# Project Init Rules

- Never guess the stack; ask when it is not in the request/spec and cannot be inferred.
- Keep the scaffold minimal and idiomatic to the stack. A working skeleton beats a fancy one.
- One project per directory; never nest a new project inside an existing instance.
- Confirm the project name and location with the user before writing anything.
- Report the created tree and the next steps at the end.